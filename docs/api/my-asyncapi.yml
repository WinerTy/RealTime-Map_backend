asyncapi: 2.6.0
info:
  title: Hello world application
  version: '0.1.0'

channels:
  hello:
    subscribe:
      summary: Получить приветственное сообщение.
      message:
        payload:
          type: string
          pattern: '^hello .+$'