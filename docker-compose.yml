services:
  backend:
    build: .
    container_name: realtime-map-backend
    ports:
      - "8001:8001"
    
    command: ../.venv/bin/python main.py
    depends_on:
      - redis
  redis:
    image: redis:latest
    container_name: redis-service
    ports:
      - "6379:6379"
